import logging

logging.basicConfig(filename='binanceOrdersLog.log', level=logging.INFO, format='%(levelname)s /  %(asctime)s  message:   %(message)s    /   (%(filename)s:%(lineno)d)')

log = logging
